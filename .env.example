# =============================================================================
# Down-Nexus API 环境配置文件
# =============================================================================
# 复制此文件为 .env 并根据您的环境修改配置值
# 注意：.env 文件已被 .gitignore 忽略，不会提交到版本控制

# -----------------------------------------------------------------------------
# PostgreSQL 数据库配置
# -----------------------------------------------------------------------------

# 数据库服务器地址
# 默认值: localhost
# 说明: 如果 PostgreSQL 运行在其他服务器，请修改为对应的 IP 地址或域名
DB_HOST=localhost

# 数据库端口号
# 默认值: 5432
# 说明: PostgreSQL 默认端口，如已修改请对应调整
DB_PORT=5432

# 数据库用户名
# 默认值: downnexus
# 说明: 用于连接数据库的用户，需要具有对应数据库的访问权限
DB_USER=downnexus

# 数据库密码
# 默认值: downnexus
# 说明: 数据库用户的密码，生产环境请使用强密码
DB_PASSWORD=downnexus

# 数据库名称
# 默认值: downnexus
# 说明: Down-Nexus 使用的数据库，需要提前创建
DB_NAME=downnexus

# SSL 模式
# 可选值: disable, allow, prefer, require, verify-ca, verify-full
# 默认值: disable
# 说明: 
#   disable - 禁用 SSL（开发环境推荐）
#   require - 要求 SSL 连接（生产环境推荐）
#   verify-full - 完全验证 SSL 证书（最高安全性）
DB_SSLMODE=disable

# 数据库时区
# 默认值: Asia/Shanghai
# 说明: 影响时间戳的存储和显示，请根据您的地理位置设置
DB_TIMEZONE=Asia/Shanghai

# -----------------------------------------------------------------------------
# 服务器配置
# -----------------------------------------------------------------------------

# API 服务器监听端口
# 默认值: 8081
# 说明: Down-Nexus API 服务器的监听端口，避免与常用端口冲突
SERVER_PORT=8081

# -----------------------------------------------------------------------------
# 安全配置（可选）
# -----------------------------------------------------------------------------

# JWT 密钥（如果启用认证）
# 说明: 用于生成和验证 JWT 令牌的密钥，生产环境请使用强随机字符串
# JWT_SECRET=your-super-secret-jwt-key-here

# API 访问日志级别
# 可选值: debug, info, warn, error
# 默认值: info
# LOG_LEVEL=info

# 跨域请求允许的源（CORS）
# 默认值: * (允许所有源)
# 生产环境建议指定具体域名，如: https://yourdomain.com
# CORS_ALLOWED_ORIGINS=*